<blockquote>
<p>상속</p>
</blockquote>
<p>extends</p>
<p>속성과 기능을 물려받는다.</p>
<p>부모 클래스-&gt;자식 클래스</p>
<blockquote>
<p>객체지향적인 방식</p>
</blockquote>
<p>객체지향은 중복 코드를 없애기 위해서.
객체지향적인 구조를 위해서 일부러 중복코드를 허용함</p>
<p>// &lt;다형성&gt; -&gt; 오버라이드(재정의), 오버로딩</p>
<p>// 자바에서도 프롭드릴링이 있나 ?  // 복잡한 상속</p>
<p>// 복잡한 상속</p>
<p>// 다중상속은 불가하다.  예외) interface-implements는 가능하다.</p>
<p>// 추상메소드 ( abstract ) - // 의도 : 구현 하지 마세요</p>
<p>// 형 변환 (리모컨) </p>