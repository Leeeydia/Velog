<p>1.java?
2.java 입출력
3. 변수
4.조건문
5.연산자
6.반복문
7.문제풀이 방식 변화
8.느낀점</p>
<h3 id="1-java-">1. Java ?</h3>
<blockquote>
<p>자바는 썬에서 개발한 객체지향 언어이다. 가정용 단말기에 적용햐려는 목적으로 1992년에 만든 '오크(OAK)언어'에서 비롯되었다. </p>
</blockquote>
<h4 id="2-java-입출력">2. Java 입출력</h4>
<p><strong>출력</strong>
값이나 문자열을 확인하기 위해 메서드를 활용하여 출력한다.</p>
<pre><code>System.out.print()   -&gt; 소괄호 안의 내용을 단순 출력
System.out.println() -&gt; 줄바꿈 (\n)
System.out.printf()  -&gt; 지시자(Specifier)와 플래그를 이용해 변수의 값을 여러 형식으로 출력</code></pre><h3 id="3-변수">3. 변수</h3>
<h4 id="변수란-">변수란 ?</h4>
<p>프로그래밍 언어에서 <strong>데이터를 저장할 수 있는 메모리상의 공간</strong> 을 의미한다.</p>
<p>변수에는 데이터 타입 (정수, 실수, 문자, 문자열, 참/거짓 등) 과 일치하는 <strong>단 하나</strong>의 값만 <strong>저장</strong>할 수 있다</p>
<p>즉 프로그램에 의해 할당받은 메모리 공간을 의미한다</p>
<h4 id="기본형--참조형">기본형 / 참조형</h4>
<p><img alt="" src="https://velog.velcdn.com/images/leeeydia/post/dd4bd51a-3f25-42dc-a06f-153e39d8b7e8/image.png" /></p>
<p>기본형 - 계산을 위해 실제 값을 저장
참조형 - 객체의 주소를 저장</p>
<h4 id="기본형-타입">기본형 타입</h4>
<p><img alt="" src="https://velog.velcdn.com/images/leeeydia/post/831e8934-61b2-4e15-897f-3989f78c766c/image.png" /></p>
<p><strong>논리형</strong></p>
<pre><code>boolean isFun = true

//값-&gt; T or F 로만 나온다</code></pre><p><strong>문자형</strong></p>
<pre><code>char a = 'b'
//값 -&gt; 작은따옴표를 이용하여 한 글자만 사용한다.
//큰 따옴표 사용시 에러가 뜬다</code></pre><p><strong>정수형</strong></p>
<p><strong>실수형</strong></p>
<h4 id="느낀점">느낀점</h4>
<ul>
<li><p>이전에 프론트엔드 과정만 배웠을 때는 인강으로만 들어서 이해가 안 된 부분은 대충 넘어가서 제대로 학습하지 못 했는데 
실강을 들으면서 직접 코드를 치고 문제를 해결하니까 제대로 코딩 배우는 느낌 ... 이 들었다</p>
</li>
<li><p>TIL의 중요성 다시 느끼기 / 잔디를 매일매일 심어보자 !
이력서를 쓰면서 느낀점이 개인 블로그나 깃허브를 통해 배운점을 기록해놔야지<br />&quot;제가 이만큼 성장해왔습니다~&quot;
라는 부분을 어필할 수 있었다.</p>
</li>
</ul>
<p>그에 비해 작년의 나는 그저 포트폴리오 제작을 위해서만 노력했기 때문에 TIL/잔디를
꾸준히 채우지는 못했었다.</p>
<p>올해는 TIL과 깃허브를 꾸준히 관리하는 것이 나의 목표 ...</p>
<ul>
<li>AI 멀리하기
알고리즘을 풀거나 모르는 것이 생기면 바로 지피티를 이용하곤 했었는데
설명을 들으면 그때 당시엔 이해 됐었으나 다음 날 되면 까먹는게 일상 ... </li>
</ul>