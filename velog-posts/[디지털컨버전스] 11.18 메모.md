<h3 id="이차원배열">이차원배열</h3>
<blockquote>
<p>int[][] numArr = new int[행길이][열길이];</p>
</blockquote>
<p>2nd array</p>
<p>int[][] arr2 = new int [2][3]; (0<del>1)/(0</del>1) -&gt; int가 3개씩 2묶음
arr2[0][0] = 1
arr2[0][1] = 2
arr2[0][2] = 3
arr2[1][0] = 4
arr2[1][1] = 5
arr2[1][2] = 6</p>
<h3 id="변수">변수</h3>
<p>배열을 사용하지 않고 변수 사용하는 이유 -&gt;
인스턴스 변수 ( new~ 객체화된 실질 데이터 느낌!)
ㄴ만들어진 객체</p>
<p>클래스 == 인스턴스 설계도 =객체 설계도 == 설계도 == 폴더 설계도 
(만들어진) 객체 == 인스턴스 == 폴더
객체 안에 있는 변수처럼 작동하는 공간 == 인스턴스 변수</p>
<p>지역번수의 죽음 -&gt; 함수 끝
인스턴스의 죽음 -&gt; 인스턴스가 끝날 때 끝 -&gt; 나랑 연결된것이 없을 때</p>
<p>int[] person1 = new int[(3)] -&gt; 다양한 타입 
// person1[0] = 12 --&gt; 데이터의 의미 x</p>
<p>// 23강,24강 복습</p>
<p>변수의 생명주기
0  // null 값 </p>
<p> 객체 -&gt; 폴더</p>
<p> 객체(object)
 는 속성과 기능으로 나뉜다.</p>
<p> 클래스의 속성
 속성 (상태, 단순 데이터) - field - 변수
 기능 (행동, 실행 데이터) - method - 함수</p>
<h4 id="this">this</h4>
<p>  메소드는 인스턴스 변수에 접근이 불가능하다
  this 를 거쳐야지 사용 가능함</p>