<p>// 자바의 기본형 </p>
<ol>
<li>기본</li>
<li>타입</li>
</ol>
<p>byte bite
컴퓨터 기본 구조 !</p>
<p>// 자료형, 메모리
// 지역공간</p>
<p>// 배열 
// new - 나는 객체 하나 만들겠다
// int[] arr = new int []   // 정수 &quot;들&quot;과 연결 될 array
int[] arr = new int[3]</p>
<p>// 프로세스 - 실행중인 프로그램
// 값 복사, 주소 복사</p>
<p>sout(arr) -&gt; 주소값알려줌</p>
<p>객체 외부에서 바라보는 것이 없을 때
해당 객체의 메모리는 해제가 된다 ?  ? ? ? ?  ?</p>
<p>// garbage collection</p>
<p>// 지역변수 -&gt; 함수가 끝나면 제거
// 객체 -&gt; 아무도 나를 보지 않아 -&gt;  GC (바로 삭제되지는 않음/ 어느정도 쌓이면 삭제)</p>
<p>// &quot;문장&quot;+숫자 -&gt; 문장화
// 식을 외우지말자 ~</p>
<p>// 기본 -&gt; 변수에 지겁 값이 담긴다
참조(객체) -&gt; 레퍼런스(리모컨, 주소)에 의해서 참조된다</p>
<p>// 객체</p>
<p>new 설계도();
new 집();</p>
<p>int[] numArr = new int[3]{10,20,30}
numArr[0] = 10</p>
<p>관련있는 데이터를 묶을 수 있는 수단이다.
장점 : 정보를 묶음으로서 프로그래밍의 복잡도가 낮아짐.</p>
<p>// 객체지향언어 (OOP)</p>
<p>객체 = 폴더
관련있는 데이터를 묶는다. ex) 계절관련 사진 폴더
무조건적으로 묶는건 x</p>
<p>실체 : 실제로 존재하는 것
주체 : 내가(실체) 나(실체)를 가리킬 때
객체 : 내(실체)가 다른 거(실체) 가리킬 때</p>
<p>나의 게임 캐릭터 하나 생성
내가 머릿속으로 세상에 존재하지 않는 책 한권 작성</p>
<p>유/무형 =&gt; 노상관 (머릿속에서 만들든 실체든 객체로 ㅇㅇ)</p>
<p>객체화 =? 객체로 만든다 </p>
<p>&lt;참고&gt;
<a href="https://blog.itcode.dev/posts/2021/08/07/what-is-oop">https://blog.itcode.dev/posts/2021/08/07/what-is-oop</a></p>
<p>// 수업 메모</p>
<pre><code>package org.example;

import java.sql.SQLOutput;
// 배열 안의 값들의 총합, 평균을 구하는 코드를 짜보세요
class Main {
    public static void main(String[] args) {
     int a= 20000;
     int [] arr= new int[3];
     arr[0]=10;
     arr[1]=20;
     arr[2]=30;

        System.out.println(arr[0]);
        System.out.println(arr[1]);
        System.out.println(arr[2]);

        System.out.println(arr[0] +arr[1]+arr[2]);


//        System.out.println(arr[0]+arr[1]+arr[2]/3);
        for(int i = 0; i &lt; arr.length; i++)
        {
            arr[i]=(i+1)*10;
        }


        int sum= 0;  // 빈상자 필요


        for(int i = 0; i &lt; arr.length; i++)
        {
            sum += arr[i];
        }
        int avg = arr.length;





//        int[] arr; // '정수들(int[])'만 연결할 수 있는 arr라는 변수를 만들고,
//        arr = new int[4]; // arr라는 변수에 int[4](설계도)로 만든 객체의 리모컨을 넣겠다.

    }

}</code></pre><pre><code>boolean arr0 =[true];
    boolean arr1 =[false];
    boolean arr2=[true];

    boolean[] arr = new boolean[];
</code></pre>