<h3 id="db">DB</h3>
<p><code>Redis</code> 
메모리 기반 / 빠름 / RAM 기반 / 단순함 / sub DB로 많이 사용</p>
<p><code>SQL</code>
데이터베이스 단위
테이블
타입은 작을 수록 좋다</p>
<p>Describe
Set
SELECT
DELETE FROM _;
WHERE id = _;
ALTER TABLE article ADD COLUMN regDate DATETIME AFTER id
DESC article</p>
<p><code>primary key</code>
not null + unique</p>
<h3 id="배운-점">배운 점</h3>