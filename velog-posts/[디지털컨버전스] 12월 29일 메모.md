<blockquote>
<p>태그</p>
</blockquote>
<p>순서 x ul(unordered list)
순서 o li(
ol</p>
<p>ul과 ol의 자식으로는 li만 올 수 있다. (div나 a태그가 올 수 없음)</p>
<blockquote>
<p>제목 태그</p>
</blockquote>
<p>h1 ~ h6 까지 존재
h7부터는 없음 !</p>
<p>nav.menu-box-1&gt;ul&gt;li*4&gt;a[href=&quot;#&quot;]{메뉴 아이템 $ }   + tab</p>
<h4 id="여러개의-메뉴탭-다양한-색깔-지정하기">여러개의 메뉴탭 다양한 색깔 지정하기</h4>
<h4 id="최상위-태그에-다른-클래스를-부여하자">최상위 태그에 다른 클래스를 부여하자</h4>
<pre><code>nav class = &quot;menu_box_1&quot;
nav class=&quot;menu_box_1 color_blue&quot;</code></pre><p>menu 박스에 다른 클래스명 부여</p>
<blockquote>
<p>안보였던 div 나오게</p>
</blockquote>
<pre><code class="language-html">
  &lt;a href=&quot;#&quot;&gt;버튼&lt;/a&gt;
  &lt;div&gt;안녕&lt;/div&gt;
  &lt;div&gt;잘가&lt;/div&gt;     

</code></pre>
<pre><code class="language-css">div:nth-of-type(1){
    display:none;
}

a + div{
    color:red;
    display: none;
}

a:hover + div {
    display:block;
}</code></pre>
<blockquote>
<p>참고 문법</p>
</blockquote>
<ul>
<li><p>CSS :nth-of-type() 가상 클래스 – 같은 이름 태그 형제 중 n 번째 요소 선택</p>
</li>
<li><p>인접동생 +</p>
</li>
<li><p>display none -&gt; block으로 바뀌게</p>
</li>
</ul>
<blockquote>
<p>트러블슈팅</p>
</blockquote>
<pre><code class="language-html">&lt;section&gt;
  &lt;div&gt;&lt;/div&gt;
  &lt;div&gt;&lt;/div&gt;
  &lt;div&gt;&lt;/div&gt;
  &lt;div&gt;&lt;/div&gt;
&lt;/section&gt;
</code></pre>
<pre><code class="language-css">section {
  width:200px;
  height:200px;
  border:10px solid red;
  font-size:0;
}

section &gt; div {
  width:50%;
  height:50%;
  background-color:black;
  display:inline-block;
  vertical-align: top;
}

section &gt; div:nth-child(2) {
  background-color:green;
}

section &gt; div:nth-child(3) {
  background-color:pink;



}

section &gt; div:nth-child(4) {
  background-color:gold;
  display: inline-block;
}
</code></pre>
<p>Q. font 사이즈를 0px로 주니까 사각형이 위로 올라갔는데 왜 그런거지 ?
A. div 총 4개, 부모의 크기를 참고해야함
정렬도 안됨
주석 처리도 안됨
inline block으로는 정교한 디자인 불가능 -&gt; 글자 취급 된다 ?
공백도 글자 취급하니까 공백의 크기를 줄이기 위해 font-size를 0으로 취급</p>
<p>만약 div에 글자가 있다면 ?
section의 font size 가 0이기 때문에 오류
div 에 vertical align top 주면 됨
부모는 0 주고 div에는 font size 주면 글자 나옴!</p>
<p>수직정렬
vertical-align 별의 위치 생각하기</p>
<p>// 방법 시도
공백 줄이기
폰트사이즈
주석처리
너비 계산기</p>
<p>수직정렬 하려면 기준을 잡아줘야한다
mdn 문서 참고하기</p>
<p>CSS 자주 쓰는거 저장해두는게 좋음</p>
<blockquote>
<p>반응형 CSS</p>
</blockquote>
<p>감성프로그래밍
lorem </p>
<blockquote>
<p>inline css</p>
</blockquote>
<p>style 속성</p>
<blockquote>
<p>뷰포트</p>
</blockquote>
<p> CSS
 노말라이즈
 라이브러리
 커스텀</p>
<p> vertical align 방식을 라이브러리 화 하자
 inline grin 방식</p>
<p> box-sizing mdn 문서 참고</p>