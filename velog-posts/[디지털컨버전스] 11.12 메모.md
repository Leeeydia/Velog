<p>sout + tab</p>
<p>System.out.println();</p>
<p>System.out.print</p>
<p>문자열은 &quot;&quot; (o)
숫자는 &quot;&quot; (△)</p>
<p>숫자를 &quot;&quot;로 감싸도 가능</p>
<p>자동정렬 c + s + f</p>
<p>print 자동 줄바꿈 x
\n  역슬래시 사용시 강제 줄바꿈
println 자동 줄바꿈 o</p>
<p>변수 정리 
x=5 // x라는 변수(저장공간)에 5라는 (숫자) 값 (데이터)을 저장(넣겠다)
y= x+10  // x: 값(변수x) y:변수
System.out.println(y);  y:값</p>
<p>연산자 정리
/ 몫 
% 나머지 
<a href="https://growingarchive.tistory.com/246">https://growingarchive.tistory.com/246</a></p>
<p>자바 int 정수만 넣을 수 있음</p>
<p>int x=5  오직 정수만 넣을 수 있는 x라는 이름의 변수(저장공간)에 5라는 값을 넣겠다
int y =10.5 (x) 실수 불가 
실수 = double
double = 10.5 (o) 실수 가능
double = 3 (o)
실수의 범위가 더 넓어서 ㄱㄴ (3.0)</p>
<blockquote>
<p>변수의 활용 알고리즘 풀이</p>
</blockquote>
<pre><code>package org.example;

// 문제 : a와 b가 가지고 있는 값을 서로 뒤바꿔주세요.

class Main {
    public static void main(String[] args) {
        // `int a` =&gt; a라는 이름의 변수(즉 공간)을 만든다.
        // `int` =&gt; a라는 공간에는 정수만 담을 수 있다.
        // `;` =&gt; 문장이 끝남을 알린다.
        int a;
        a = 5;

        System.out.println(a);
        System.out.println(a + 10);

        int b = 10;

        // `+` =&gt; 문장과 문자을 합친다.
        System.out.println(&quot;a : &quot; + a);
        System.out.println(&quot;b : &quot; + b);

        // 여기서 부터
         int tmp = a;
         a = b;
         b = tmp;
        // 여기까지 수정 가능

        System.out.println(&quot;a : &quot; + a);
        // 출력 : a : 10
        System.out.println(&quot;b : &quot; + b);
        // 출력 :  b : 5
    }
}</code></pre><p>조건문 정리 
불리언 boolean
if ()가 참(true)이라면 {} 실행하겠다</p>
<p>if(age&gt;=20){
}</p>
<p>조건문 비교연산지 정리
피연산자 != 헷가ㄹ림
같지 않으면 참 
같으면 거짓</p>
<p>&lt;오답&gt;
  // <code>!=</code> =&gt; 같지 않다.
        if ( a != 10 ) {
            System.out.println(&quot;f&quot;);
        } xxx</p>
<p>   if ( 20 &gt; 2 &amp;&amp; 10 &gt; 3 &amp;&amp; true != false &amp;&amp; 10 != 10 ) {
            System.out.println(&quot;t&quot;);
        }</p>